var animation = lottie.loadAnimation({
    container: document.getElementById('10xfaster'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '10xfaster.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('serverless'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'serverless.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('scale'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'scale.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('integration'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'integration.json'
})