var animation = lottie.loadAnimation({
    container: document.getElementById('fast'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'fast.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('serverless'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'serverless.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('backlog'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'backlog.json'
})

var animation = lottie.loadAnimation({
    container: document.getElementById('integrate'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'integrate.json'
})